import { Change1Directive } from './change1.directive';

describe('Change1Directive', () => {
  it('should create an instance', () => {
    const directive = new Change1Directive();
    expect(directive).toBeTruthy();
  });
});
